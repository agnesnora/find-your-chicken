import React from "react";

const SearchForm = () => {
  return (
    <div>
      <form>
        <input placeholder="Enter keyword or location" />
        <button>Search</button>
      </form>
    </div>
  );
};

export default SearchForm;
